<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.ReplyBoardMapper">

	<select id="list" resultType="domain.ReplyVO">
		select
		*
		from
			board_reply
		where boardNo =
		#{boardNo}
		order by replyNo desc
	</select>

	<insert id="create">
		insert into board_reply (boardNo, replyContent, nickname,regDate,updateDate)
		values (#{boardNo},#{replyContent},#{nickname},sysdate,sysdate)
	</insert>

	<update id="update">
		update board_reply set replyContent = #{replyContent},
		updatedate = sysdate
		where replyNo = #{replyNo}
	</update>

	<delete id="delete">
		delete from board_reply where replyNo = #{replyNo}
	</delete>


	<select id="listPage" resultType="domain.ReplyVO">
		<![CDATA[

		select
		*
		from
			board_reply
		where replyNo
			BETWEEN ((#{page}-1)*10+1) AND (#{page}*10)
		order by replyNo desc
		]]>
	</select>

	<select id="count" resultType="int">
		select count(boardNo) from board_reply where boardNo = #{vo.boardNo}
	</select>


</mapper>

